import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  textFieldHalfWidth: {
    width: '21ch',
    background: 'white',
  },
  textFieldFullWidth: {
    width: '45ch',
    background: 'white',
  },
}));

export default useStyles;
